[pytest]
testpaths = .
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    e2e: end-to-end browser tests (requires running app stack)
    slow: long-running browser seed or integration tests
addopts = -v --tb=short
